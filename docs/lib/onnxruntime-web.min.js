// ONNX Runtime Web - Placeholder
// In production, download from: https://cdn.jsdelivr.net/npm/onnxruntime-web@latest/dist/ort.min.js
console.log('ONNX Runtime Web placeholder loaded');
window.ort = {
    InferenceSession: {
        create: async (path) => {
            console.log('Mock ONNX session created for:', path);
            return {
                run: async (feeds) => {
                    console.log('Mock ONNX inference run');
                    return { output: new Float32Array([0.1, 0.2, 0.3]) };
                }
            };
        }
    },
    Tensor: class {
        constructor(type, data, dims) {
            this.type = type;
            this.data = data;
            this.dims = dims;
        }
    }
};